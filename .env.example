# Client-side (safe to expose in browser)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-side only (never expose in browser)
SUPABASE_SERVICE_ROLE_KEY=
PLANT_API_KEY=

# Set to "true" during local dev to skip Plant.id API calls and return a fixture response
USE_MOCK=true
